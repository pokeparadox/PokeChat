### TODO LIST

Items that should be expanded into separate plans for later implementation.

## LIST

- When helping bot classify a word, you need to allow all word types. The user can also say they don't know. If LLM is active we can use the LLM to classify if the user does not know.

