{
  "web_search": {
    "enabled": false,
    "timeoutMs": 10000,
    "apiKey": "${WEB_SEARCH_API_KEY}",
    "endpoint": "https://api.duckduckgo.com/?q={query}&format=json"
  },
  "read_url": {
    "enabled": false,
    "timeoutMs": 15000
  },
  "shell_command": {
    "enabled": false,
    "timeoutMs": 10000,
    "allowedCommands": [
      "ls", "pwd", "whoami", "date", "uptime", "uname",
      "echo", "cat", "wc", "du", "df", "which", "env"
    ]
  }
}
